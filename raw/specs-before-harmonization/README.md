# Superseded specification snapshot

These eight documents preserve the exact pre-harmonization bytes. Their original relative links are historical and may not resolve here; use the corresponding active files under `docs/specs/` and the root roadmap. No old approval, data-scope or model claim overrides the active suite. See `manifest.json` for source paths and hashes.
